[project]
name = "video-pipeline"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "anthropic>=0.120.2",
    "google-genai>=2.16.0",
    "pillow>=12.3.0",
    "python-dotenv>=1.2.2",
    "questionary>=2.1.1",
    "requests>=2.34.2",
]

[project.scripts]
video-pipeline = "video_pipeline.main:main"
voicevox-synthesize = "video_pipeline.synthesize_audio:main"
render-video = "video_pipeline.render_video:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
