[project]
name = "video-pipeline"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "anthropic>=0.120.2",
    "python-dotenv>=1.2.2",
    "python-pptx>=1.0.2",
]

[project.scripts]
video-pipeline = "video_pipeline.main:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
